namespace SpaceDiscordBot.Modules
{
    internal enum ChannelScope
    {
        //Central Guild Channels

        //Company Channels
        AnnouncementChannel,
        PublicConsoleChannel,
        AdminConsoleChannel,
        DataChannel,

        //Player channels
        PlayerLobbyChannel,
        NotificationsThread,
        AdventureThread,
        MissionsThread,
        HomeSystemThread,
        ColonySystemsThread,
        OutpostThread,
    }
}
